
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div id="user-info"></div>
    <h1>PSA Argentina</h1>
    <div id="cart-container"></div>
    <a href="/carts/{{cartId}}">Ir al carrito (<span id="cart-count">{{cartCount}}</span>)</a>

    <div class="slider">
        <div class="gallery">
            {{#each products}}
            <div class="card">
                <h2>{{this.title}}</h2>
                <img src="{{this.image}}" alt="{{this.title}}" style="max-width: 100%; height: auto;">
                <p>Precio: ${{this.price}}</p>
                <a href="/products/{{this._id}}" class="btn">Ver Detalle</a>
            </div>
            {{/each}}
        </div>
    </div>

    <a href="/products" class="btn ver-todo">Ver Todo</a>

    <script src="/js/home.js"></script>

    <style>
        .slider {
            display: flex;
            overflow-x: auto;
            padding: 20px;
        }
        .gallery {
            display: flex;
            flex-wrap: nowrap;
        }
        .card {
            background-color: #f4f4f4;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin: 10px;
            padding: 20px;
            min-width: 200px;
            text-align: center;
        }
        .btn {
            background-color: navy;
            color: white;
            padding: 10px;
            text-decoration: none;
            border-radius: 5px;
        }
        .ver-todo {
            display: block;
            margin-top: 20px;
            text-align: center;
        }
    </style>
</body>
</html>
